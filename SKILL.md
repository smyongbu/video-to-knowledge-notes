---
name: video-to-knowledge-notes
description: 将视频转换成独立、完整、适合长期学习的简体中文“文字＋带原声的短视频＋视频下方讲解文字＋截图”知识笔记。当用户说“把视频转换成笔记”“视频转笔记”“整理这个视频”“总结这个视频”“分析这个视频”“把课程做成笔记”“根据视频生成教程”或意思相近的话时，默认使用此 Skill。保留课程原有教学顺序和操作脉络，删除口语与重复内容，整理标题并核验必要事实；连续操作和需要听讲解的知识点优先直接从原视频剪成带声音 MP4，不为成品视频新增、烧录或叠加字幕，而是把校正后的语音识别文字紧接在对应视频下方；同一连续源画面中的裁剪中心或倍率变化默认使用短促、缓入缓出的平滑运镜，逐帧保证活动 UI 完整，不用裁剪硬切、溶解或混帧伪造流畅；可按内容或用户要求自定义分辨率，静态界面使用清晰局部截图。面向零基础读者，凡正文描述视频中可见的界面、按钮、菜单、对象、状态、变化或结果，只要存在清晰对应画面就必须配置邻近媒体。
---

# 视频转知识笔记

把视频视为知识来源，重新创作为即使没看过原视频也能独立阅读的教程。

开始任务后，必须完整阅读 [references/obsidian-note-spec.md](references/obsidian-note-spec.md)，并遵守其中的写作、制图、文件组织和 950px 视觉验收规范。

没有可靠字幕、需要本地语音识别或视频较长时，使用 [scripts/resumable_transcribe.py](scripts/resumable_transcribe.py) 分段转写。先在独立虚拟环境中安装 [scripts/requirements.txt](scripts/requirements.txt)，再为每个源视频指定独立工作目录。不得使用“全部识别完成后才首次写文件”的一次性转写方式。

凡使用 FFmpeg 做静音检测、抽取音频、抽帧、裁剪、运镜、转码或完整解码验收，必须通过 [scripts/safe_ffmpeg.py](scripts/safe_ffmpeg.py) 串行启动，不得直接或并行启动 `ffmpeg.exe`；[scripts/resumable_transcribe.py](scripts/resumable_transcribe.py) 的内部 FFmpeg 调用也必须走同一守卫。该守卫在启动前检查物理内存与提交额度；Windows 下先把暂停创建的 FFmpeg 加入内核 Job Object，再恢复执行，4.5 GiB 时提前停止、5 GiB 为单进程硬上限，监控失效或系统余量跌破安全线也会失败关闭并终止整个 Job。不得降低门槛或绕过守卫来赶进度。完整规则与失败后的分段恢复方式见 [references/obsidian-note-spec.md](references/obsidian-note-spec.md) 的“FFmpeg 安全执行与防崩门槛”。

## 工作流程

1. 检查视频、字幕、讲义和参考资料是否可读取。视频是主要知识来源；字幕只用于检索和辅助理解。为每个视频创建独立、稳定的临时工作目录，不把中间文件混入最终附件目录。
2. 没有可靠字幕时，先运行可续跑转写脚本。把约 5 分钟作为目标分段长度而不是硬切点：先在目标点前后搜索自然停顿，优先在静音区间中点切分；附近没有合适停顿时才按目标时间回退，并在相邻分段保留重叠上下文。不得为了严格控制时长而把一句话或一个连续讲解机械劈开。每完成一段立即原子写入该段结果、总体部分转写稿和进度清单。确认 `progress.json`、`transcript.partial.txt`、正常运行日志与错误日志已经出现后，再等待长时间识别。任务中断后使用相同视频、工作目录和参数续跑，只处理未完成分段；不得删除有效断点或从头重做。随后提取课程实际讲授的步骤、概念、案例和操作结果，删除寒暄、宣传、口头禅、重复内容和无关过渡。
3. 对关键事实、术语、争议内容及可能过时的信息进行核验。优先使用官方、权威和原始资料，明确标注无法确认的内容。
4. 沿用课程的教学顺序和操作脉络，把相邻的重复内容合并，并用多个一级标题划分主要教学阶段、二级和三级标题继续细分。文件名承担整篇文档标题的作用，不要先写一个包住全文的 `# 文档总标题`，再把全部正文降成其下的二级标题；也不要把整篇内容塞进单个一级标题。禁止生成“课程目标”“学习目标”“本节目标”“复习清单”“学习清单”“检查清单”或意思相近的开场、结尾栏目，也不要在文末附带任务式复习勾选框；只有用户明确要求这些栏目时才允许加入。不要添加时间戳，不要把内容压成松散摘要，也不要为了追求知识体系而彻底打乱课程顺序。
5. 以讲解者实际说的话作为笔记主干：逐句识别其中的概念、判断、操作指令和结果说明，删除口头禅与重复后改写成忠实、自然的书面语，不得只凭画面概括成稀疏提纲。撰写面向完全零基础读者的简体中文教程。每个操作按需要交代目标、前置条件、入口与位置、具体动作、可见结果、功能含义和容易误解之处，使读者无需观看视频也能跟做；不要扩写与课程无关的大量背景知识。界面中的英文按钮、菜单项、标签、状态名、权限值、功能名称和专业术语，第一次出现在标题、正文或图注时统一写成 `English（中文翻译）`，例如 `Move file（移动文件）`、`can edit（可编辑）`；后续若只写英文会妨碍零基础读者理解，则继续保留括号翻译。产品名可保留品牌原名，但首次出现时补充用途翻译，例如 `Figma Sites（网站制作）`。不要给网址、代码、文件路径、普通英文句子或已经紧邻中文解释的专有名称机械加括号；翻译必须结合当前界面语境，不能只做字面直译。
6. 在剪辑前建立“讲解句/动作—画面阶段—媒体”覆盖表，逐句、逐动作扫描字幕、语音与画面。覆盖表中的每个静态媒体候选都必须先标记画面类型、媒体决策和决策理由；类型至少区分“必须保留原画面”“默认必须重绘的抽象图解”“辅助视觉”和“无法确认”，媒体决策至少区分“截图”“重绘”“辅助新增”和“舍弃”。不得让未分类、无理由的静态图片进入成品。原则上“一小段文字说明一个独立概念或连续操作”，紧跟对应媒体。凡连续点击、拖拽、绘制、动画、手势、结果变化，或讲解声音本身有学习价值的内容，默认直接从原始视频剪成带原声 MP4；不得以无声预览或其他二次处理文件作为剪辑源。每段从完整语句前的自然停顿进入，在讲解和结果稳定后的自然停顿结束，不截断词句；保留完成当前知识点所需的最短充分内容，通常约 8～45 秒，必要时可更长。知识点较多时增加短视频数量，不用少数长视频笼统覆盖多个步骤；同一知识点的入口、动作和结果尽量由一段视频连续呈现。静态界面、对象或状态再按画面分类选择局部截图或重绘图。

   MP4 必须保留源视频中与该片段同步的有效声音，默认使用 H.264 视频、AAC 音频、`yuv420p`，并启用 fast start；禁止删除音轨、用静音占位或从无声二次素材转码。除非用户明确要求，不改变语速、音高和播放速度，不降噪到损伤语音。若源片段确实没有有效声音，才允许无声输出，并在交付时说明。

   每段短视频都必须围绕当前教学目标裁剪有效画面，禁止只把原视频整帧等比缩小后交付。操作画面的裁剪窗口默认以鼠标在当前完整操作过程中的有效位置、有效轨迹和整体操作范围作为主锚点，而不是只看某一帧的鼠标坐标：先观察本段鼠标是否只在中间编辑区操作，还是在工具栏、编辑区、属性栏之间连续往返，再确定能够覆盖完整操作链的稳定取景范围。鼠标用于判断“这个镜头应该拍哪里”，不用于驱动摄像机实时追针；鼠标在同一教学区域内移动、悬停、点击或拖动时，优先保持一个完整覆盖操作目标、路径和结果的固定构图。与此同时，入口、点击目标、已展开菜单、子菜单、弹窗、工具栏、属性栏、编辑对象、拖拽路径和结果区域的联合边界是不可突破的完整性约束；镜头可以不把鼠标放在正中央，但不得为了追鼠标而把正在使用的 UI 裁成一半。只有教学重点真正切换到无法由当前构图清楚容纳的远距离区域时才换镜。同一段连续源画面内，只要裁剪中心或倍率需要改变，默认不得把裁剪参数台阶式硬切；使用一次短促、单向、缓入缓出的平滑移动。远距离跨区优先采用“当前稳定特写 → 平滑拉远 → 短暂稳定全景 → 平滑推近 → 新稳定特写”，让读者保持空间定位。只有原片本身已经切镜、前后画面不存在可连续的空间关系，或经过逐帧证明任何平滑安全路径都会裁断活动 UI 时，才允许在语义边界硬切，并在镜头计划中记录理由。只有片段本身正在讲解整个界面的空间关系，而且裁剪会破坏教学意义时，才暂时保留全界面；后续具体操作必须恢复局部稳定构图。若同一语义范围天然很窄或很小（例如竖向侧栏、小菜单、小弹窗），不要把无关空白或整个编辑区一起塞进画面来维持全景；允许只保留目标周围必要的方向性上下文，也允许在讲解上下半部时拆成两个稳定特写。不得把硬字幕、方便定位或分辨率已经缩小当作不裁剪的理由；源视频已有硬字幕可以随有效画面自然保留或被裁掉，但不得拼接字幕条或附加画面底栏。

   对所有操作类片段，把鼠标实际操作范围作为裁剪主体和主要构图依据，而不是把“鼠标是否经过”当作其他画面能否出现的硬性准入条件。关键交互时刻必须把鼠标箭头、被点击、双击、悬停、拖拽、滚动或输入的目标，以及从按下到释放的有效路径完整剪进画面；鼠标点击某个按钮时，该按钮和点击动作不得位于画面外或被边缘截断。先确定本段全部有效鼠标位置、操作目标所属的完整 UI 功能单元、直接触发的 UI 和必须观察的结果，再用它们的联合边界构图。这里的“完整 UI”是鼠标当前操控区域所属的完整功能容器或相互关联的 UI 组合，不是强制保留整个软件窗口：菜单要保留触发入口、父菜单、子菜单、全部边界和当前项；弹窗、下拉列表、属性面板、工具栏或工具分组要保留当时可见的标题、外框、控件和必要关联内容；画布操作要保留完整被操作对象、有效路径、直接结果和必要定位上下文。任何正在使用的 UI 都不能只显示一半。

   在完整 UI、鼠标动作、操作结果和稳定构图都满足之后，鼠标没有操作到且与当前步骤关系较弱的工具栏、属性栏、画布、空白区和其他面板应尽量少纳入画面，但这只是构图优化，不是强制删除条件。为了保持鼠标操控区域的整个 UI 完整、说明界面关系、补足 16:9 或避免镜头抖动，可以保留必要的相邻未操作区域；完整 UI 的优先级始终高于缩小无关区域。裁剪只能用一块连续的矩形窗口改变取景范围，不得通过遮罩、涂抹、打黑块、挖空、拼贴或局部删除画面来清除未操作区域，也不得修改源视频内容。纯讲解整体界面空间关系、全程没有具体鼠标操作的非操作类概览片段可以保留全景。

   正常软件教学视频默认采用“稳定镜头为主、必要换镜为辅”的镜头语法；允许并优先让整段或长时间保持同一位置和倍率，不得为了显得有剪辑而机械制造变焦、平移或呼吸式缩放。一个语义操作通常只使用 1 个稳定构图；确有远距离跨区、入口与结果不能同时清楚容纳时才拆成 2～3 个稳定镜头。当前教学目标必须足够大：主要按钮、菜单文字、弹窗字段、属性值或操作对象在 1280×720 成品中应可直接辨认，不能让目标只占左上角或中央的一小块；若在 100% 成品视图中仍需寻找或放大才能看清，即使镜头稳定也判定为失败，必须进一步推近、重定中心或拆镜。当前有效 UI 的完整边界决定允许使用的最大放大倍率：鼠标悬停并展开菜单、子菜单、下拉列表、弹窗或属性面板后，必须把触发入口和正在阅读、点击的完整 UI 一并纳入 16:9 裁剪框，并保留可辨认的安全边距。若当前倍率无法完整容纳 UI，立即选择更小倍率或更合适的取景中心，不得交付只显示半个菜单、半个面板、缺失菜单标题、缺失当前选项或被画面边缘切断的界面。UI 打开和尺寸稳定后保持到相关讲解或操作完成；关闭 UI 且教学重点真正转向下一区域后才允许换镜。

   对编辑软件，不预先把中间编辑区、周边工具栏和属性栏全部收入镜头，而是先按鼠标在本段实际操作到的区域确定取景重点。鼠标只在编辑区内操作时，重点展示完整被操作对象、鼠标动作和直接结果；在不破坏完整 UI、定位关系和稳定构图的前提下，尽量减少未参与的工具栏或属性栏。从工具栏选择工具后回到编辑区操作时，工具按钮所属的完整可见工具栏或工具分组、鼠标路径和编辑区对象都属于本次操作范围；若它们能在一个可读构图中完整出现，就全程固定该构图，若不能，就在选取工具完成后切到编辑区的第二个稳定镜头。在编辑区与属性栏之间调整参数时，完整保留鼠标所操作的属性面板或功能区、被编辑对象和直接变化结果；只有两侧距离过远且文字无法读清时，才在明确语义边界换镜。工具栏、编辑区、属性栏三者确实连续参与同一操作时，按整段有效轨迹及各自完整 UI 边界构图；没有参与的区域只需在可行时尽量减少，不得为了排除它而裁断正在使用的 UI。必要时把操作拆为少量独立镜头，不得用摄像机持续横穿三者来代替镜头设计。

   把“鼠标整体操作范围为主、有效鼠标主锚点、完整 UI 硬边界、语音与画面用于判定意图”作为统一决策规则。先从高频画面中提取本段鼠标的整体活动范围、跨区域路径、位置、停留点、点击、拖拽和悬停触发的 UI 变化，再结合逐句转写判断操作链及视觉重点。鼠标稳定停在对象上、沿明确路径移动、点击后触发状态变化或悬停后展开界面时，视为有效信号并优先用于取景。无意义乱晃属于少见例外：只有鼠标在短时间内明显来回摆动、绕圈、抖动、快速扫过无关区域、等待加载时乱晃，且移动没有对应讲解和 UI 变化时，才视为无效噪声；此时冻结在最后一个稳定构图或当前完整 UI 上，不跟着来回晃动。不得因为担心少见的乱晃而削弱正常鼠标轨迹对整体构图的主导作用。

   稳定指向、有效悬停、菜单选择、点击、拖拽对象、拉出参考线和调整节点首先用于计算本镜头的整体取景范围，不自动触发镜头移动。即使标记为 `follow`，也只表示鼠标目标可触发“下一稳定镜头”的候选，不表示逐帧跟随；仅当新的教学目标在当前安全构图外持续成立、并且语音或 UI 变化确认了语义转场时才启动一次换镜。镜头围绕“鼠标＋当前目标＋完整菜单或面板＋必要结果”的联合边界取景，而不是把鼠标箭头锁死在画面中央。鼠标在当前操作区内移动、暂时离开目标、点击后停住、移动到菜单边缘或出现短暂抖动时，保持镜头完全稳定。

   在镜头计划中为每个时间段记录“讲解主题、鼠标整体操作范围、逐个交互目标、跨区域操作链、目标区域、鼠标操控区域所属的完整 UI 边界、活动 UI 联合边界、直接结果区域、可尽量减少的弱相关区域、必须保留的定位上下文、画面阶段、鼠标有效性、稳定构图、目标倍率、换镜方式、转场安全走廊、锚定边、过渡时长、静止保持时长和硬切例外理由”。跨区域操作链至少说明本段是否涉及 `工具栏→编辑区`、`编辑区→属性栏` 或 `工具栏→编辑区→属性栏`，并列出哪些区域由鼠标实际操作、哪些未操作区域可在不破坏完整 UI 的前提下尽量少纳入。鼠标状态至少区分 `ignore（少见的无效乱晃）`、`reference（用于构图，不移动镜头）` 和 `follow（确认下一个远距离教学目标，可触发一次换镜）`。默认先按鼠标整体操作范围和完整 UI 边界设计静态镜头，再通过停留、点击、拖拽、UI 变化和语音一致性确认是否真的需要第二镜头。每段视频的静止画面应占绝大多数时长，单个稳定保持通常不少于 2 秒，教学动作期间优先保持 3～8 秒；约 30 秒内通常不超过 2～3 次镜头变化。同一连续源画面中的裁剪换镜默认使用平滑移动，裁剪位置或倍率硬切默认判定为失败；常规过渡约 0.4～0.8 秒，远距离跨区最多约 0.8～1.2 秒，使用单调、无回弹的缓入缓出曲线，过渡完成后必须精确停住。不得把两个关键点之间的整个时间段全部插值成运动，也不得在没有新教学重点时连续缓慢漂移、反复推拉、同时平移与缩放或用多个相近倍率制造呼吸感。

   为每次平滑运镜逐帧计算一块连续的 16:9 裁剪矩形；转场中的每一帧都必须满足活动 UI 联合边界，而不是只检查起点和终点。靠近源画面边缘的目标要锚定对应边：例如下方工具栏要锁定裁剪框底边，左上菜单要锁定左边与上边；随着当前裁剪宽高推导位置，不得把起点和终点的 `x/y` 独立线性插值，否则中间帧可能越过安全边界。若两个特写之间没有一条既可读又安全的直接路径，先平滑拉远到能完整容纳两区的全景，稳定停留到界面状态切换完成，再平滑推近新目标。平滑只改变连续矩形取景，不使用交叉溶解、帧混合、光流补帧、重复帧、运动模糊或改变播放速度；原片自身的界面切换发生时保持裁剪框稳定，使其读作原操作变化而不是镜头跳变。

   画面分析必须分层进行，禁止只按固定的低频抽帧结果直接生成运镜。第一层对完整视频默认每 5 秒抽取一张概览帧，用于建立界面、场景和主要阶段的全局索引；5 秒概览只能帮助定位候选片段，不能作为最终裁剪、变焦或鼠标跟随的唯一依据。第二层在每个待剪片段内结合逐句语音和词级时间，至少每 0.5～1 秒检查一次画面，识别菜单开合、选择状态、对象移动、参数变化和结果出现的准确阶段。第三层对点击、拖拽、绘制、节点调整、参考线移动、弹窗切换、短暂提示和其他快速变化，在动作前后执行逐帧或足够高频的连续检查，同时提取鼠标轨迹和活动 UI 的完整外接边界，确定鼠标意图、动作开始、关键路径、UI 最大展开范围、完成时刻和结果稳定区间。

   如果 5 秒概览帧之间出现语音主题变化、鼠标有效移动、界面突变或无法解释的前后差异，必须回到该时间范围提高采样频率，不得猜测中间发生了什么。最终镜头边界必须来自片段内高频或逐帧分析；稳定区间使用完全不变的裁剪位置与倍率，只有明确过渡区间才在源帧率或成品帧率上生成连续曲线。不能把稀疏关键点直接做成台阶式跳变，也不能为了避免跳变而把整个相邻区间都变成缓慢运动。全局概览只负责导航。为控制工作量，可以只对最终入选的短视频执行高频分析，但每个交付视频都必须完成该分析和连续播放验收。

   成品短视频默认输出为 1280×720（16:9、720p）；用户明确指定其他分辨率时才改用用户要求。先依据鼠标实际操作范围、鼠标操控区域所属的完整 UI、完整交互目标和直接触发结果确定最小充分联合框，再用一块连续矩形裁剪窗口向外扩展到 16:9 并缩放到 1280×720，编码尺寸必须为偶数。扩展时尽量少带入未操作区域，但允许为保证完整 UI、定位关系和稳定构图保留必要的相邻界面；不得为了排除未操作区域而裁掉 UI 的标题、外框、边缘、关联控件或展开内容，也不得使用纯色边带、局部遮罩或拼贴来代替正常取景。若一个可读镜头无法同时容纳操作链中的多个完整 UI，就在语义边界拆成多个稳定镜头。不得通过拉伸破坏画面比例；源视频有效分辨率不足时使用保守缩放，不得用生成式方法补写界面文字或细节。凡正文中的可视描述都必须在邻近位置配置对应 MP4 或截图；一段媒体不能清楚覆盖多个对象时，拆成多个“短说明＋短视频/小截图”单元。

   不为剪辑后的 MP4 新增、烧录、叠加或生成字幕，也不默认交付旁挂字幕文件。语音识别结果只作为笔记文字来源：每个有语音的视频嵌入后，必须立即放置与该片段对应的“视频讲解文字”。先按片段起止时间选取逐句转写，再人工校正人名、产品名、按钮名、专业术语、数字和标点，删除寒暄、口头禅、无意义重复及识别噪声，但不得遗漏概念、判断、操作指令和结果说明。

   视频下方的讲解文字使用自然、连贯的书面语，不添加时间戳，不逐句堆砌成字幕格式，不使用每两三秒一行的短句，也不写“视频中说”“讲解者提到”等依赖原视频的表述。可根据内容整理为一至数个短段落、步骤列表或必要的提示块；若正文在视频上方已经完整表达同一段语音，可以把视频下方文字写成精确的补充或合并后的完整讲解，但不能让视频下方为空，也不能机械重复完全相同的文字。视频与讲解文字之间不得插入其他主题的图片、标题或段落。
7. 按“是否需要保留原画面信息”选择配图方式。凡是界面位置、操作步骤、实物外观、作品细节、数据、实验现象或其他视觉细节会影响学习结果时，使用视频原画面的局部截图。若画面的核心信息仅由可辨认的文字、节点、箭头、线框、简单图标、颜色分组或层级组成，所有关系都能逐项核对，且原始手写笔迹、粗糙排版、纹理和精确外观本身没有教学价值，则归类为“可无损重绘的抽象图解”，默认必须使用 Mermaid、SVG、确定性矢量工具或 PPT 式图解重制，不得仅因原截图尚可辨认就继续使用手绘截图；需要稳定兼容时可导出为 SVG 或 PNG。图解即使出现在 Figma、FigJam、PPT、白板或其他软件画布中，也应按正在讲解的是“软件操作”还是“图内知识关系”分类，不能因为带有软件界面背景就自动保留截图。只有原始布局、笔迹、外观、精确数据或视觉细节本身具有教学价值时才允许保留原截图，并在覆盖表中逐图写明不可替代理由。辅助图标或补充示意图能明显提升理解时也可以加入，但不能替代原始证据或操作画面。详细判定见 [references/obsidian-note-spec.md](references/obsidian-note-spec.md) 的“配图判断”。
8. 默认以源视频所在目录作为最终交付目录。将 `<主题名称>.md` 直接放在视频同一层级，不要再创建“主题名称/主题名称.md”式的外层主题文件夹；配图放入同目录的 `attachments/<主题名称>/`，并在 Markdown 中使用相对路径。普通截图和独立图片统一使用最简单的标准 Markdown 语法 `![说明](attachments/<主题名称>/图片名.png)`，不得用 `<div>`、`<img>`、`style`、`width`、`align`、HTML 表格、Base64 或 CSS 包裹和排版这些图片。真正用于表达流程、关系或结构的 Mermaid 代码块、内联 SVG 图表可以直接保留在笔记中，但不能用来包装普通截图或控制普通图片布局。若同时处理多个视频，为每篇笔记使用独立的附件子目录，避免图片重名。只有用户明确指定其他位置时才改变此规则。
9. 在 Obsidian 阅读视图或能实际解析最终 Markdown 的像素等效预览中，将正文宽度固定为 950px、缩放设为 100%，从头到尾检查标题、正文、表格、图片、图注、链接以及整体排版是否正确；此步骤只做排版、媒体引用和可用性验收，不参与判断截图要不要 AI 高清化。

   默认在后台完成验收：直接读取最终 Markdown 和附件，执行引用、哈希与双向清单审计，并在不抢占前台窗口的本地渲染器或无头浏览器中生成能实际解析最终 Markdown 的 950px 像素等效预览。不得仅为例行验收而启动、切换、聚焦或控制用户的 Obsidian，也不得向其发送鼠标或键盘输入。只有用户明确要求在真实 Obsidian 中检查，或已发现只能在 Obsidian 内复现的具体显示问题且用户明确同意控制软件时，才允许操作 Obsidian；若检测到用户正在使用该窗口，立即停止自动操作并改用后台证据，不抢占窗口。
10. 对每张最终导出的栅格图片文件单独以 100% 原始像素比例目视检查，不通过 Obsidian、浏览器缩放、Markdown 显示宽度或图片嵌入效果判断清晰度。这里的 100% 必须是 1 个图片像素对应 1 个屏幕 CSS 像素，关闭适应窗口、自动缩放和插值预览；图片超出屏幕时按原比例滚动或分块覆盖整张图。PNG、JPG/JPEG、WebP 等逐张检查；动图逐帧或至少覆盖全部不同画面状态。只要当前最终候选本身出现文字发虚或粘连、笔画断裂、图标块化、锯齿、压缩噪声，或其他肉眼可见的模糊，就必须进入清晰度修复；反之，图片文件本身清楚时不机械增强。是否高清化不得依据原图尺寸、成图尺寸、显示宽度、DPI、像素密度或放大倍率，这些数据也不得作为清晰度触发条件。AI 高清化默认只改善清晰度，不改变最终图片分辨率：处理过程中允许临时放大供模型修复，但交付前必须恢复为输入图片完全相同的像素宽度和高度；只有用户明确要求改变分辨率时才允许输出其他尺寸。SVG 等矢量图片不执行栅格 100% 或 AI 高清化，改为在最终自然显示尺寸和 2 倍渲染尺寸逐项检查文字、线条、图标和边界。
11. 清晰度修复顺序固定为：从前一帧、当前帧、后一帧选择最清晰原帧 → 收紧裁剪或拆图 → 若当前最终候选仍有哪怕轻微模糊，必须尝试保真 AI 超分模型。若换用相邻原帧、重裁或拆图后已经完全清楚，则不再机械运行 AI。优先使用不做语义重绘的超分模型；含文字的软件 UI、数据图表、地图或其他精确证据画面如果使用提示式图像生成或扩散模型，只能作为候选，必须通过下述严格核对才能使用，不能因表面更锐就直接交付。将高清化结果与未处理原帧在两者各自 100% 原始像素比例、等尺寸比较视图、200% 放大比较视图和 50% 半透明叠加视图中核对，逐项检查文字笔画、按钮名称、数字、图标、颜色、边界和空间位置，可辅以 OCR，但必须人工目视确认。只有增强前已能从相邻原帧确认全部字符、增强后逐字逐数一致，并且高清化后的图片文件本身在 100% 原始像素比例下确实更清晰时才允许使用。任何伪文字、新增或缺失笔画、涂抹、光晕、过度锐化、颜色改变、边界位移或位置改变都视为失败；立即弃用增强图，改用其他模型、其他源帧、更保守处理或拆图。仍无法可靠恢复时，该图不得进入成品；若它不可替代且删除会造成可视描述缺图，不得宣称任务完成，应明确报告阻塞并请求更清晰源素材，不猜测或生成缺失细节。
12. 在最终 950px 阅读视图逐张检查截图的构图和排版。先框出相邻文字真正讲解的最小教学目标，再从目标边界向外只保留约 8～24px 或不超过短边 10% 的必要边距；目标本身而不是“目标加上下文”必须成为画面绝对主体。按钮、图标、菜单项、面板或列表内容在紧裁图中原则上应占宽度和高度各至少 60%，目标与必要上下文合计至少 90%，无关区域不超过 10%。若保留上下文会让目标变小，就拆成一张定位图和一张细节图。凡读者第一眼仍需搜索目标、图片只在角落有信息，或排版上需要点击放大才能定位，都必须继续裁剪、重新取帧或拆图；是否执行 AI 高清化仍只看第 10 步对图片文件本身的 100% 检查。对于天然很窄或很长的侧栏与列表，允许保留自然显示宽度，不得为了填满 950px 正文而横向拉伸。

   第 9～12 步合并构成每次任务都不可跳过的“最终图片终审关卡”。必须等全部图片资产写入最终附件目录后，直接审核该目录中的实际成品；候选图、缓存或工作区副本不能代替最终文件。在临时工作目录维护逐图结构化审核记录，至少包含相对路径、格式、画面类型、媒体决策、决策理由、保留原截图的不可替代理由（如适用）、最终文件 SHA-256、实际像素宽高或 SVG 视口、950px 阅读视图自然显示宽高、类型对应的默认尺寸区间及例外理由、100% 原始像素或矢量双倍率检查结果、构图判定、修复轮次、AI 尝试与接受／拒绝原因、Markdown 引用次数和最终状态。每次换帧、重裁、拆图、增强或覆盖后都要重新读取最终目录、更新哈希并重做终审。分别枚举 Markdown 的全部图片引用与附件目录中的全部图片资产，引用缺失和无引用残留都必须为 0。反向检查是否仍有满足默认强制重绘条件的板书、手绘关系图、简单流程图或 PPT 图解被错误保留为截图；存在且没有逐图不可替代理由时，终审直接失败。只有未审核数、待修复数和失败数都为 0，全部图片引用均存在，且当前最终候选仍有轻微模糊的栅格图都已尝试保真 AI 高清化并完成逐字、逐数字、逐图标、逐边界、逐颜色和逐位置核对，才允许交付；未通过保真核对的 AI 候选必须拒收。最终答复报告审核总数、换帧／重裁／拆图数量、AI 尝试数、AI 接受数、AI 拒绝数和最终通过数，不在成品目录生成审核文件。
13. 完成“图文覆盖、媒体类型、术语翻译、简洁源码与零基础复现”审查：逐个二级、三级标题以及每段、每条列表核对，确保所有可视描述都有相邻配图，入口、动作和结果没有只写文字；逐张复核静态媒体分类，确认满足默认强制重绘条件的板书、手绘关系图、简单流程图和 PPT 图解已经重制，保留原截图的项目均有不可替代理由；扫描标题、正文、表格和图注中的英文界面词，确认按钮、菜单、标签、状态、权限、功能名和专业术语首次出现时都带有准确的括号中文翻译，且同一术语译法前后一致；扫描 Markdown 源文件，确认普通截图和独立图片只使用 `![说明](相对路径)`，不存在用于普通图片布局的 HTML、CSS 或尺寸属性；Mermaid、内联 SVG 等源码只在其本身就是知识图表时保留，不得用于包装截图。确认标题分类清楚、顺序与课程一致，并让未看过视频的初学者仅凭笔记即可完成同样的学习过程。若表格过窄、列宽失衡，或出现标签残片、溢出、异常空白，也必须修正并重新渲染。
14. 写入完成后，从源视频所在目录重新独立枚举一次文件，确认 Markdown 与视频处于同一层级、所有附件引用都存在且数量一致。不得只根据创建或复制命令的成功返回判断交付完成；网络共享目录也必须重新读取验证。若目录不可写，明确说明原因并请求新的交付位置，不要静默改到工作区或临时目录。
15. 对每个 MP4 用媒体探测工具复核视频与音频流、编码、时长、尺寸、帧率和音画同步。逐段以 1× 速度完整连续播放，不能只抽查起点、中点、终点或少量关键帧；静态联系表只能检查构图和 UI 完整性，不能代替运镜节奏验收。确认开头与结尾没有截断语句，声音可听、无异常静音，画面没有跳帧、重影、双轮廓或融化变形；不通过补帧、混帧、重复帧或改变播放速度制造流畅感。确认成品没有新增字幕流、旁挂字幕、烧录字幕或为了保留硬字幕而拼接的字幕条。逐段对比源视频帧与成品帧，确认成品确实执行了有效区域裁剪，而不是只缩放整帧，并保留可复现的固定镜头范围、换镜点和过渡参数。对每次裁剪中心或倍率变化按成品帧率逐帧检查：同一连续源画面中的裁剪硬切数必须为 0，除非符合已记录的硬切例外；过渡曲线必须连续、单调、无回弹，起步和停稳没有顿挫，且每个中间帧都完整容纳当时活动 UI、鼠标目标和必要结果。逐个复核点击、双击、悬停、拖拽、滚动和输入动作，确认鼠标、操作目标、从按下到释放的有效路径以及鼠标所操控的整个 UI 功能单元始终完整在画面内；菜单链、工具栏或工具分组、面板、弹窗、下拉列表不得缺少标题、外框、边缘、当前项或必要关联控件。再反向检查画面中的弱相关工具栏、属性栏、画布、空白区和其他面板，确认在不影响完整 UI、定位关系和镜头稳定的前提下已经尽量减少，而不是要求全部删除。确认裁剪始终是连续矩形取景，没有遮罩、黑块、挖空、拼贴或局部删除。连续检查整段镜头：鼠标的整体操作范围应决定稳定取景，有效鼠标不等于实时追踪；画面绝大多数时间必须静止，只有教学重点跨区时才短暂换镜。检查是否存在肉眼可见的抖动、逐格挪动、持续漂移、无意义推拉、反复放大缩小、突然跳焦、在停住前回弹或全程过宽导致重点太小；任一情况出现都必须重做。逐项检查已展开菜单、子菜单、下拉列表、弹窗和属性面板，确认标题、边框、当前选项、鼠标目标与必要定位环境完整可见，没有任何 UI 只显示一半；若 16:9 裁剪框无法容纳，必须降低倍率或拆镜重做。逐个核对 Markdown 视频嵌入，确认其下方紧接对应的讲解文字，语义覆盖该片段的概念、操作与结果，术语已经人工校正，没有时间戳、识别乱码、字幕式碎句或与视频无关的文字。在 950px 像素等效预览中确认视频可直接播放、控件可用、主要界面文字与视频下方讲解文字均可辨；只有符合第 9 步的明确授权条件时才改在 Obsidian 阅读视图中确认。核对 Markdown 中每个视频引用均存在，并确认每个有声源片段的输出都包含 AAC 或其他 Obsidian 可播放的有效音轨。

## 转写断点与日志

- 把 `progress.json` 作为恢复依据，把 `chunks/` 中的逐段 JSON 作为已完成工作的事实来源。`transcript.partial.txt` 和 `transcript.partial.json` 必须在每段完成后更新；全部完成后才原子改名为 `transcript.txt` 和 `transcript.json`。
- 在进度清单中记录每个内部边界来自 `silence`（自然停顿）还是 `time_fallback`（未找到停顿后的时间回退）。若时间回退边界附近仍跨越长句或连续讲解，整理转写稿时必须联合阅读相邻两段，并根据重叠内容恢复完整语义。
- 长时间加载模型或转写单段时持续写入心跳日志。正常启动、分段进度、耗时和完成状态写入 `logs/运行.log`；警告、中断、异常和堆栈只写入 `logs/错误.log`，两类日志用同一操作编号关联。
- 首段完成后立即检查逐段结果、部分转写稿和进度清单能否重新读取。若识别进程消失但没有完整转写稿，先读取错误日志和断点状态，再原命令续跑；不要把“进程已停止”误判为“已经完成”。
- 用户在处理中询问文件位置或进度时，明确区分最终交付目录与临时工作目录，并报告已完成分段数。中途问答不得主动终止后台识别；若运行环境确实终止了任务，下一次继续时先从断点恢复。
- 最终笔记与附件验证成功后，才允许清理临时 WAV；默认保留转写稿、进度清单和两类日志到本次交付结束。清理仅限当前视频的明确工作目录，不递归删除宽泛目录。

## 执行原则

- 不把任务降级成逐字转写、普通摘要或时间轴整理。
- 不写“视频中提到”“讲解者说”“某分某秒”等依赖原视频的表述。
- 不编造事实、来源、案例或图表关系。
- 对补充背景、合理推论、争议观点和无法确认的信息作出清楚区分。
- 按视频顺序制作的界面与实操教程默认采用高视觉覆盖率：只要正文内容能由视频中的清晰画面帮助定位、理解或复现，就配上邻近图片。宁可使用多张与文字紧邻的小截图，也不要为了少放图而合并成一张难以阅读的大图；纯抽象文字不机械添加无关装饰图。
- 逐段、逐句、逐条列表检查可视名词和可见变化。描述四个界面区域、四个选项或四种状态时，必须让四项都分别有图，或使用一张能把四项全部清楚展示的紧凑截图；只显示其中一项的图片不能算覆盖整组文字。
- 把“忠实保留原意”放在视觉美化之前。若重绘可能改变布局、按钮位置、视觉层级、数据、外观、步骤关系或任何教学细节，就不要重绘。
- 不只软件 UI 需要保留原画面。实物、作品、地图、实验装置、演示结果、摄影画面、精确图表和其他依赖视觉细节的内容，同样优先使用原画面局部截图。
- 截图保留完成学习任务所需的最小充分上下文：既裁掉字幕、水印、头像、播放器控件和无关区域，也要保留足以定位按钮、菜单、面板与操作结果的周边环境。
- “足以定位”不等于保留完整侧栏、整块画布或整列面板。正文只讲一个按钮、图标、菜单项或版本条目时，以目标边界为基准紧裁，只留一两个相邻锚点；需要更大空间关系时另做一张定位图，不得用大范围截图代替细节图。
- 小范围截图必须单独打开图片文件并按 100% 原始像素比例检查。优先截取视频解码得到的原始帧，不使用播放器窗口截图；同一目标连续查看相邻帧，选择对焦最好、运动最少、压缩伪影最轻的一帧。只要图片文件本身仍有轻微模糊，就必须尝试保真 AI 超分辨率或高清化；是否高清化不参考 Obsidian 显示效果、图片尺寸或放大倍率。增强前后必须并排核对文字、图标轮廓、颜色、边界和位置，发现任何臆造或改写立即弃用。AI 处理可以临时放大，但默认交付尺寸必须与处理前完全一致，不得把“高清化”自动解释成提高最终分辨率。
- 全界面截图只用于说明整体空间关系。若一张全景同时包含三个或更多需要阅读的区域，或缩到正文宽度后侧栏、按钮、文字明显变小，必须把左侧栏、画布、工具栏、右侧栏等拆成多张局部图，并分别配一句功能说明；全景图最多作为辅助定位，不能承担细节教学。
- 每张截图都要在最终渲染前后各审查一次构图。目标本身原则上应占图像宽度和高度各至少 60%，目标与必要上下文合计至少 90%，无关空白、背景、重复列表或未讲解区域不超过 10%；空间布局本身是教学对象时才可例外。窄菜单、弹窗和侧栏不要连同大块空白画布或整条长列表一起保留，有用信息只在角落或中部时必须围绕它重新裁剪。
- 版本历史说明只截历史面板标题、当前版本和正文正在解释的版本条目，不保留旁边大画布或面板下方空区；主菜单入口说明只截左上角图标、提示名称及一两个定位锚点，不保留完整 Pages、Layers 侧栏和空画布。类似场景一律按此判断。
- 纯文字表格不能替代需要视觉定位的真实画面。表格可以负责精确比较，但当表格行对应界面区域、按钮、菜单或可见结果时，还必须在相关文字附近放置对应的小截图。
- 图片与文字必须成组出现：一句简短文字只说明一个动作或一个可见变化，紧接对应局部截图，再用纯文本图注点明截图证明的操作或差异。不要连续堆图，也不要让图片与解释相隔多个段落。
- 成品笔记中的普通截图和独立图片只写一行标准 Markdown 图片语法，图注另起一行写普通正体文本，例如 `图：Slides 用于创建演示文稿。`。图注不得使用 `*图：……*`、`_图：……_`、引用嵌套或其他会让文字倾斜、变形的格式。不得为了居中、缩放、并排或加边框而插入 HTML、CSS 或 SVG 包装代码；需要控制视觉大小时，直接把图片文件裁成合适画布并输出足够清晰的像素尺寸。真正承担知识表达的 Mermaid 流程图、关系图或内联 SVG 矢量图可以保留源码，并按正常图表同样检查准确性和可读性。
- 对创建文件、打开入口、选择菜单、切换选项、执行操作和观察结果等可视步骤，只要视频中有清晰画面，就不要留下只有文字而没有截图的操作段落。
- 抽象关系图、流程图、板书或 PPT 图解在所有节点、标签、箭头、数值和层级都能被准确辨认、能够逐项核对，且原始笔迹、外观与排版本身没有教学价值时，默认必须重绘为清晰的 PPT/矢量风格图解；存在任何歧义，或原始外观本身承载教学信息时使用原图裁剪，并记录理由。
- 可以增加语义图标、编号、箭头或高亮框帮助阅读，但不得遮挡原内容、伪造视频信息或用图标替代必须看清的真实画面。
- 当讲解者依次点名菜单项、模板、按钮、图标或可见对象时，即使没有发生点击，也要把每个被点名对象写成独立的“短说明＋紧裁小图”单元；不要把它们只整理成干巴巴的文字列表。例如同时讲到 Slides 与 Buzz 时，应分别截取 Slides 和 Buzz 的清晰小图，或使用一张两者都能直接读清的紧凑局部图。
- 一张图只解决一个主要问题；是否配图取决于理解收益，而不是正文能否勉强用文字说明。
- 多张图属于同一操作时，优先分别裁成可读的小图并逐张说明；只有每个面板在 950px 正文内仍然清楚时才合成长图或对比图。不要制作只有一小块有用、其余大面积空白的纵向或横向长图。
- 图已清楚表达的内容不在正文中机械复述。
- 若用户明确要求简短摘要、逐字稿或时间轴笔记，遵从用户要求，不强制执行完整重创作流程。
- 临时转写、抽帧、渲染预览和处理中间文件可以放在工作区，但最终 Markdown 必须写回源视频同目录。不要移动、重命名或覆盖源视频，也不要覆盖同目录中与本次任务无关的笔记和附件。

## 交付说明

最终答复简要列出与源视频同目录的主笔记文件、实际使用的局部截图或重绘图、重要核验或校正、无法确认的信息，以及已完成的图片文件 100% 清晰度检查和 950px 排版检查。路径应指向已重新读取验证的最终文件，不要只提供工作区中的中间产物。
